# Variables de entorno para SQL Playground

# URL de conexión a PostgreSQL (Railway)
# Formato: postgresql://usuario:password@host:puerto/database
DATABASE_URL=postgresql://postgres:tu_password@tu-proyecto.railway.app:5432/railway

# Entorno (development o production)
NODE_ENV=production

# Puerto del servidor (Railway lo asigna automáticamente)
PORT=3000
